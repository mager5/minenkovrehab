[variables]
NODE_ENV = "production"

[phases.setup]
nixPkgs = ["nodejs", "npm"]
apt = []

[phases.install]
cmds = ["npm install"]

[phases.build]
# No build step needed for Express API

[start]
cmd = "npm start"
runImage = "node:18-alpine"